# 🎯 Guess the Number Game with Python + CustomTkinter

This project is a fun desktop application built using Python and CustomTkinter, where users try to guess a randomly generated number. Players can select different difficulty levels, receive feedback on their guesses, and track their best scores for each difficulty.

---

## 🚀 Features

🎲 Random number generation based on selected difficulty (Easy, Medium, Hard) <br>
📥 Input field to enter your guess <br>
🔘 Dropdown menu to select difficulty level <br>
🎯 "Check Guess" button to validate guesses <br>
📊 Displays number of attempts made <br>
🏆 Tracks and displays best scores for each difficulty <br>
❌ Error handling for invalid (non-numeric) input <br>
🛑 Attempt limits for Medium and Hard difficulties <br>
🪟 Clean and modern UI using CustomTkinter <br>
🔄 "Reset Game" button to start over without restarting the app

---

## 🖥️ Technologies Used

- Python 3.x
- CustomTkinter (for modern GUI interface)
- Tkinter messagebox for alerts and notifications
- PyCharm or VS Code recommended

---

## 📂 Project Structure

- **main.py**: Main application script. Handles both UI layout and game logic. Key responsibilities:

🖼️ Builds GUI layout using frames, labels, buttons, entry fields, and dropdown menus <br>
🎯 Implements number guessing logic and checks user input <br>
📊 Tracks attempts and updates best scores dynamically <br>
❌ Handles invalid inputs and shows alert messages <br>
🔁 Provides a reset function to restart the game <br>
🧠 Includes difficulty settings with different ranges and attempt limits

---

## 🛠️ Setup

### Step 1: Clone the Repository

To get started, clone this repository to your local machine using the following command:

`git clone https://github.com/your-username/GameGuessingNumber.git`

### Step 2: Dependencies

Make sure you have Python 3.x installed. Install CustomTkinter if you don’t have it:

`pip install customtkinter`

### Step 3: Run the project

Run the main Python script to launch the game:

`python3 main.py`

---

## 🤝 Contributing

Contributions are welcome! If you'd like to improve the project, feel free to:

1. Fork the repository.
2. Create a new branch (`git checkout -b feature-name`).
3. Make your changes and commit them (`git commit -m 'Add new feature'`).
4. Push to your branch (`git push origin feature-name`).
5. Submit a pull request.

If you find bugs or have feature requests, please [open an issue](https://github.com/ximenes13/GameGuessingNumber/issues).
